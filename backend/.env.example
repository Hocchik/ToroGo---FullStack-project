# Example environment variables for backend
PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/torgo_db
JWT_SECRET=replace_this_with_a_strong_secret

# Optional: nodemon, etc
NODE_ENV=development
